SafeContext Privacy Gate / validate feature

Upload/replace files at exactly these paths:

NEW:
  src/safecontext/validate.py
  tests/test_validate.py
  tests/test_cli_validate.py

REPLACE:
  src/safecontext/cli.py

Do not upload these .py files to the repository root.

Feature:
  safecontext validate <file>

Behavior:
  SAFE    -> prints SAFE TO SHARE and exits 0
  BLOCKED -> prints detection counts only (never raw values) and exits 1

Suggested commit:
  feat: add privacy validation gate

After commit, run SafeContext Tests in GitHub Actions.
